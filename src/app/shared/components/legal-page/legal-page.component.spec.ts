import { LegalPageComponent } from './legal-page.component';

describe('LegalPageComponent', () => {
  let component: LegalPageComponent;

  beforeEach(() => {
    component = new LegalPageComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
