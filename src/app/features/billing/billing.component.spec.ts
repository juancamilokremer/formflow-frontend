import { BillingComponent } from './billing.component';

describe('BillingComponent', () => {
  let component: BillingComponent;

  beforeEach(() => {
    component = new BillingComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
