import { PublicFormComponent } from './public-form.component';

describe('PublicFormComponent', () => {
  let component: PublicFormComponent;

  beforeEach(() => {
    component = new PublicFormComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
